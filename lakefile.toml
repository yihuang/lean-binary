name = "binary"
version = "0.1.0"
defaultTargets = ["Binary"]

[[lean_lib]]
name = "Binary"

# Not in `defaultTargets`: built on demand with `lake build bench`.
[[lean_exe]]
name = "bench"
root = "Bench"

